#!/bin/sh
# Notarize.main.sh - main document command. The ActionUI window opens via the
# ACTIONUI_WINDOW definition and Notarize.main.init seeds it (including the
# opened/dropped app from OMC_OBJ_PATH). Nothing to do here for a non-blocking
# window; keep it exit 0.
exit 0
